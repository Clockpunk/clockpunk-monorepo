# Clockpunk - Монорепозиторий

Премиальный сайт бренда Clockpunk с полным управлением контентом через Sanity CMS.

## 🏗️ Структура проекта

```
clockpunk-monorepo/
├── apps/
│   ├── web/          # Next.js фронтенд
│   └── studio/       # Sanity Studio CMS
├── packages/
│   └── config/       # Общие конфигурации
└── docs/             # Документация
```

## 🚀 Быстрый старт

### Требования

- Node.js >= 18.17.0
- npm >= 9.0.0
- Аккаунт Sanity (бесплатный)

### 1. Клонирование репозитория

```bash
git clone https://github.com/Clockpunk/Clockpunk.git
cd Clockpunk
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Настройка Sanity

#### Создание проекта Sanity

```bash
cd apps/studio
npx sanity init --project-id --dataset production
```

Следуйте инструкциям CLI для создания проекта. Запомните:
- **Project ID** - идентификатор проекта
- **Dataset** - по умолчанию `production`

#### Настройка переменных окружения для Studio

```bash
# apps/studio/.env
cp .env.example .env
```

Заполните файл `.env`:

```env
SANITY_STUDIO_PROJECT_ID=ваш_project_id
SANITY_STUDIO_DATASET=production
SANITY_STUDIO_API_VERSION=2024-03-01
```

### 4. Настройка Web-приложения

```bash
# apps/web/.env.local
cd ../web
cp .env.example .env.local
```

Заполните файл `.env.local`:

```env
NEXT_PUBLIC_SANITY_PROJECT_ID=ваш_project_id
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2024-03-01
NEXT_PUBLIC_SITE_URL=https://clockpunks.ru

# Опционально: только если dataset приватный
# SANITY_API_READ_TOKEN=ваш_токен
```

### 5. Запуск в режиме разработки

Из корня репозитория:

```bash
# Запустить web-приложение
npm run dev

# В отдельном терминале - запустить Sanity Studio
npm run dev:studio
```

- **Web**: http://localhost:3000
- **Studio**: http://localhost:3333

## 📝 Управление контентом

### Первоначальная настройка в Sanity Studio

1. Откройте http://localhost:3333
2. Войдите через Google/GitHub
3. Создайте документы:
   - **Site Settings** (Настройки сайта)
   - **Home Page** (Главная страница)
   - **Artwork** (Работы)

### Создание главной страницы

1. В Studio перейдите в **Home Page**
2. Добавьте секции через кнопку **Add item**
3. Доступные секции:
   - **Hero Section** - главный экран
   - **Featured Artworks** - избранные работы
   - **Gallery Grid** - галерея работ
   - **Brand Story** - история бренда
   - **Process Section** - процесс создания
   - **Press Section** - пресса и выставки
   - **CTA Section** - призыв к действию

4. Для каждой секции заполните поля
5. Переключатель **Enabled** включает/выключает секцию
6. Перетаскивайте секции для изменения порядка

### Добавление работ (Artworks)

1. В Studio перейдите в **Artworks** → **Create new**
2. Заполните:
   - **Название** (обязательно)
   - **Slug** - автоматически из названия
   - **Статус** - draft/available/sold/archived
   - **Год создания**
   - **Материалы** - например: "Сталь, часовые механизмы"
   - **Размеры** - например: "438 мм × 360 мм"
   - **Описание** - rich text
   - **Обложка** - главное изображение (обязательно)
   - **Галерея** - дополнительные изображения
   - **Избранная работа** - показывать на главной
3. Сохраните (Publish)

### Создание дополнительных страниц

1. В Studio перейдите в **Pages** → **Create new**
2. Задайте Slug:
   - `about` - страница "Об авторе"
   - `contacts` - страница "Контакты"
   - `legal` - юридическая информация
3. Добавьте секции (аналогично главной странице)
4. Настройте SEO-параметры

## 🌐 Деплой

### Web-приложение (Vercel)

1. Создайте аккаунт на [Vercel](https://vercel.com)
2. Подключите GitHub репозиторий
3. В настройках проекта:
   - **Root Directory**: `apps/web`
   - **Build Command**: `npm run build`
   - **Output Directory**: `.next`
4. Добавьте Environment Variables:
   ```
   NEXT_PUBLIC_SANITY_PROJECT_ID=ваш_project_id
   NEXT_PUBLIC_SANITY_DATASET=production
   NEXT_PUBLIC_SANITY_API_VERSION=2024-03-01
   NEXT_PUBLIC_SITE_URL=https://ваш-домен.vercel.app
   ```
5. Deploy!

### Sanity Studio

Вариант 1: Sanity Hosting (рекомендуется)

```bash
cd apps/studio
npm run deploy
```

Studio будет доступна по адресу: `https://ваш-проект.sanity.studio`

Вариант 2: Vercel (отдельный проект)

1. Создайте отдельный проект в Vercel
2. Root Directory: `apps/studio`
3. Build Command: `npm run build`
4. Output Directory: `dist`
5. Environment Variables:
   ```
   SANITY_STUDIO_PROJECT_ID=ваш_project_id
   SANITY_STUDIO_DATASET=production
   SANITY_STUDIO_API_VERSION=2024-03-01
   ```

## 🛠️ Разработка

### Команды

```bash
# Разработка
npm run dev              # Запустить web
npm run dev:studio       # Запустить studio

# Сборка
npm run build            # Собрать web
npm run build:studio     # Собрать studio

# Проверка кода
npm run lint             # ESLint
npm run typecheck        # TypeScript проверка

# Очистка
npm run clean            # Удалить node_modules и сборки
```

### Добавление новой секции

1. Создайте схему в `apps/studio/schemaTypes/objects/mySection.ts`
2. Добавьте в `apps/studio/schemaTypes/index.ts`
3. Создайте компонент в `apps/web/src/components/sections/MySection.tsx`
4. Добавьте в `SectionRenderer.tsx`

Подробнее в [docs/content-editing.md](./docs/content-editing.md)

## 📚 Документация

- [Редактирование контента](./docs/content-editing.md)
- [Деплой](./docs/deployment.md)
- [Архитектура](./docs/architecture.md)

## 🔒 Безопасность

- ❌ Не коммитить `.env` файлы
- ✅ Использовать `.env.example` как шаблон
- ✅ Хранить секреты в Vercel Environment Variables
- ✅ Использовать публичный dataset для Sanity (если возможно)

## 📋 Чек-лист запуска

- [ ] Установлены зависимости (`npm install`)
- [ ] Создан Sanity проект
- [ ] Настроены `.env` файлы
- [ ] Studio запускается локально
- [ ] Web запускается локально
- [ ] Созданы Site Settings
- [ ] Создана Home Page с секциями
- [ ] Добавлены работы (Artworks)
- [ ] Проверены все страницы
- [ ] Настроен деплой на Vercel
- [ ] Studio задеплоена

## 🆘 Поддержка

При возникновении проблем:

1. Проверьте переменные окружения
2. Убедитесь, что Sanity проект создан
3. Проверьте версию Node.js (`node --version`)
4. Очистите кеш: `npm run clean && npm install`
5. Проверьте логи в консоли браузера

## 📄 Лицензия

Private © 2024 Clockpunk
